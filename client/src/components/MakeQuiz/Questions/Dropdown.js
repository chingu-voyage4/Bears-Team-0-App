import React from 'react';
import OptionQuestion from './OptionQuestion';

const DropDown = ({ questionData }) => {
  return (
    <OptionQuestion questionData={questionData} />
  );
};

export default DropDown;
